apiVersion: v1
kind: Service
metadata:
  name: crossplane-state-metrics
  labels:
    app.kubernetes.io/name: crossplane-state-metrics
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: crossplane-state-metrics
  ports:
    - name: metrics
      port: 8080
      targetPort: metrics
      protocol: TCP
